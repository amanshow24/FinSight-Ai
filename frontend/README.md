# Frontend

This folder is a standalone copy of the current UI app.

## Run locally

```bash
npm install
npm run dev
```

## Deploy

Point Vercel at this folder and set `VITE_API_URL` to your Render backend URL.

Keep the Firebase env values here too if you want auth to keep working.